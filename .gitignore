# Secrets
.env
.env.local

# Data and logs (machine-specific) — anchored to repo root so source
# packages like src/trading/data/ and tests/data/ are NOT ignored.
/data/
/logs/
/backups/

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
.venv/
venv/
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/

# Notebooks
.ipynb_checkpoints/

# IDE
.vscode/
.idea/
*.swp
*~

# OS
.DS_Store
Thumbs.db

# uv lockfile is committed (uv.lock); pip equivalents below are not
pip-log.txt

# Local jupyter
*.local.ipynb

# Claude Code — personal/machine-local state (settings.json is shared, tracked)
.claude/settings.local.json
.claude/agent-memory/

# 코드 그래프 인덱스·시각화 산출물 — 로컬 재생성물
# (graphify update / code-graph-mcp / trading.scripts.codemap)
graphify-out/
.code-graph/
.codemap/
